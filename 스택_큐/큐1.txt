#include <iostream>
#include <vector>
#include <queue>
using namespace std;

int main() {
    int answer = 0;
    int bridge_length = 3; 
    int weight = 30;
    vector<int> truck_weights = { 10,10,10,20,20,10 };

    queue<int> q;
    queue<int> trucks;

    for (int i = 0; i < truck_weights.size(); i++) {
        trucks.push(truck_weights[i]);
    }

    int num = 0; int cnt = 0; int k = 0; bool flag = false;

    while (!trucks.empty()) {
        if(q.front()==-1)
        if (num + trucks.front() <= weight) {
            q.push(trucks.front()); 
            if (flag == true) k = 4;
            num += trucks.front(); trucks.pop();
        }
        cnt++;
        cout << "cnt: " << cnt << endl;
        num -= q.front(); q.pop();
    }
    cnt+=2;
    answer = cnt;
    cout << answer;
}
